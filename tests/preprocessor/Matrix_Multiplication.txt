/* Multiplicación de matrices (3x2) * (2x4)
   Uso de arreglos planos (row-major) y funciones/while del lenguaje de alto nivel.
*/

#include <Matrix_Multiplication_matmul>

# Definición de dimensiones
int rowsA = 3;
int colsA = 2;
int rowsB = 2;
int colsB = 4;

# Declaración de arreglos planos
int A[6];
int B[8];
int C[12];   # resultado 3x4

# Inicializar A (3x2) con inputs
output "Ingrese la matriz A (3x2), una fila por linea (2 numeros separados por espacio):";
output "\n";
int row = 0;
int col = 0;
int temp = 0;
int idx = 0;

row = 0;
while (row < rowsA):
    col = 0;
    while (col < colsA):
        input temp;
        idx = row * colsA + col;
        A[idx] = temp;
        col = col + 1;
    endwhile
    row = row + 1;
endwhile

# Inicializar B (2x4) con inputs
output "Ingrese la matriz B (2x4), una fila por linea (4 numeros separados por espacio):";
output "\n";
row = 0;
while (row < rowsB):
    col = 0;
    while (col < colsB):
        input temp;
        idx = row * colsB + col;
        B[idx] = temp;
        col = col + 1;
    endwhile
    row = row + 1;
endwhile

# Imprimir Matriz A
output "Matriz A:\n";
row = 0;
while (row < rowsA):
    col = 0;
    while (col < colsA):
        idx = row * colsA + col;
        print(A[idx]);
        output " ";
        col = col + 1;
    endwhile
    output "\n";
    row = row + 1;
endwhile

# Imprimir Matriz B
output "Matriz B:\n";
row = 0;
while (row < rowsB):
    col = 0;
    while (col < colsB):
        idx = row * colsB + col;
        print(B[idx]);
        output " ";
        col = col + 1;
    endwhile
    output "\n";
    row = row + 1;
endwhile

# Llamar a la función de multiplicación
mat_mul(A, B, C, rowsA, colsA, colsB);

# Imprimir la matriz resultante C (3x4) por filas
output "Matriz C = A x B:\n";
int r = 0;
int c = 0;
int pos = 0;
r = 0;
while (r < rowsA):
    c = 0;
    while (c < colsB):
        pos = r * colsB + c;
        print(C[pos]);
        output " ";
        c = c + 1;
    endwhile
    output "\n";
    r = r + 1;
endwhile