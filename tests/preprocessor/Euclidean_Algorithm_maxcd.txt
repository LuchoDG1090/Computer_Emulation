func maxcd(int a0,int b0):
    int a=a0; # Variable entera a
    int b=b0; # Variable entera b
    while (a!=b):
        if (a>b): a=a-b;
        else: b=b-a;
        endif
    endwhile
    return a;
endfunc