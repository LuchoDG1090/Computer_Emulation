/* Ordenamiento Burbuja (Bubble Ordering)
   Código en el lenguaje de alto nivel usado en el proyecto.
   Implementa bubble sort con arreglos, swaps y salida de resultados.
*/

func bubble_sort(int arr[], int n):
    int i = 0;
    int j = 0;
    int swapped = 1;
    int tmp = 0;

    while (swapped != 0):
        swapped = 0;
        i = 1;
        while (i < n):
            if (arr[i-1] > arr[i]):
                tmp = arr[i-1];
                arr[i-1] = arr[i];
                arr[i] = tmp;
                swapped = 1;
            endif
            i = i + 1;
        endwhile
        n = n - 1;
    endwhile
endfunc

# Ejemplo de uso
output "Ingrese la cantidad de elementos:";
int n = 0;
input n;

# Declaramos un arreglo con capacidad dinámica
int arr[n];

output "Ingrese los elementos separados por espacio:";
int k = 0;
int val = 0;
while (k < n):
    input val;
    arr[k] = val;
    k = k + 1;
endwhile

# Ejecutar el ordenamiento
bubble_sort(arr, n);

# Imprimir arreglo ordenado
output "Arreglo ordenado:";
k = 0;
while (k < n):
    print(arr[k]);
    print(" ");
    k = k + 1;
endwhile
output "";


